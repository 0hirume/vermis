# Vermis

A lossless Luau parser written in Rust for formatting, linting, and source analysis.

Preserves source bytes, comments, and whitespace while providing structured syntax, source spans, and error recovery.
